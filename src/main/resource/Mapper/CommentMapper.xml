<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sjw.ShiroTest.Dao.CommentDao">
    <select id="getCommentsByProduct" parameterType="map" resultType="Comment">
        SELECT * FROM PRODUCT_COMMENTS WHERE PRODUCTID = #{productId, jdbcType=INTEGER}
        LIMIT 10 OFFSET #{offset, jdbcType=INTEGER}
    </select>
</mapper>